spring:
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        default_schema: password_manager
        hibernate.hbm2ddl.auto: create-drop
  datasource:
    driver-class-name: org.postgresql.Driver
